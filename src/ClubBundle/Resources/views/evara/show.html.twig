{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evara</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evAra.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ evAra.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evara_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('evara_edit', { 'id': evAra.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
