{% extends 'base.html.twig' %}

{% block body %}
    <h1>av2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av2.id }}</td>
            </tr>
            <tr>
                <th>Titleav2</th>
                <td>{{ av2.titleAv2 }}</td>
            </tr>
            <tr>
                <th>Paragraphav2</th>
                <td>{{ av2.paragraphAv2 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('av2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('av2_edit', { 'id': av2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
