{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evsup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evSup.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ evSup.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evsup_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('evsup_edit', { 'id': evSup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
