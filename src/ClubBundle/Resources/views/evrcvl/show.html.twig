{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evrcvl</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evRcvl.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ evRcvl.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evrcvl_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('evrcvl_edit', { 'id': evRcvl.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
