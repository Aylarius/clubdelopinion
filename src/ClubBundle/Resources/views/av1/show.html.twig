{% extends 'base.html.twig' %}

{% block body %}
    <h1>av1</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av1.id }}</td>
            </tr>
            <tr>
                <th>Titleav1</th>
                <td>{{ av1.titleAv1 }}</td>
            </tr>
            <tr>
                <th>Paragraphav1</th>
                <td>{{ av1.paragraphAv1 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('av1_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('av1_edit', { 'id': av1.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
