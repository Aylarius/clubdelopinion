{% extends 'base.html.twig' %}

{% block body %}
    <h1>PageMembre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pageMembre.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ pageMembre.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pagemembre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pagemembre_edit', { 'id': pageMembre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
